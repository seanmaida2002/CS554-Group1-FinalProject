version: 1
frontend:
    phases:
      preBuild:
        commands:
          - cd react_client
          - npm install
        build:
            commands:
                - npm run build
    artifacts:
        baseDirectory: react_client/dist
        files:
            - '**/*'
    cache:
        paths: react_client/node_modules/**/*
